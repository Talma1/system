


  /*const CreateUser = (user) => {
    return post(`${baseUrl}/create_user/`, user);
  }

  const UpdateUser = (permissions ,id) => {
    return put(`${baseUrl}/update_permissions_user/${id}`, {"permissions":permissions});
  }
  // delete user mathod. id required.
  const DeleteUser = (id) => {
    return deleteMethod(`${baseUrl}/delete_user/${id}`);
  }

  const CheckLogin = (user) =>{
    return post(`${baseUrl}/check_login/`, user);
  }
  
 */